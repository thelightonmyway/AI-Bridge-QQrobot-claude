[build-system]
requires = ["setuptools>=64.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-bridge-qqrobot-claude"
version = "0.1.4"
description = "AI-Bridge-QQrobot-claude monorepo - persistent CLI agent gateways for Claude Code, Codex, and AGY"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
py-modules = []

